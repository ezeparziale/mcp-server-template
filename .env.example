# Transport mode for MCP server
# Options: "stdio" or "http"
MCP_MODE=http

# Port where the MCP server will run
PORT=3000

# Allowed origins for CORS (comma-separated).
# Example:
#   http://localhost:3000,http://another-domain.com
#   *   -> allow all (only for development)
ORIGIN=

# Hosts allowed for DNS rebinding protection (comma-separated).
# Used only in HTTP mode. Host header must match one of these values.
# Example:
#   127.0.0.1,localhost,myapp.com
# In dev, usually: 127.0.0.1,localhost
# In prod, set your public domain(s).
ALLOWED_HOSTS=

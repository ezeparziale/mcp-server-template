# Transport mode for MCP server
# Options: "stdio" or "http"
MCP_MODE=http

# Port where the MCP server will run
PORT=3000

# Allowed origins for CORS (comma-separated).
# Example:
#   http://localhost:3000,http://another-domain.com
#   *   -> allow all (only for development)
ORIGIN=
